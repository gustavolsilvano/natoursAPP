import { StyleSheet } from 'react-native';

import { width, backgroundColor, height } from '../../../constant/constant';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor
  }
});

export default styles;
